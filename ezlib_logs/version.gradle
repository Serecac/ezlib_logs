allprojects {
    ext {
        versionMajor = 1
        versionMinor = 0
        versionPatch = 0
        nameApk = "Ezlib_Logs"
        versionName = "${versionMajor}.${versionMinor}.${versionPatch}"
    }
}