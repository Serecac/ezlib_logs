allprojects {
    ext {
        versionMajor = 1
        versionMinor = 0
        versionPatch = 0
        nameApk = "Ezlib_Log"
        versionName = "${versionMajor}.${versionMinor}.${versionPatch}"
    }
}